apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent-pipe-uploader.fullname" . }}-config
  labels:
    {{- include "agent-pipe-uploader.labels" . | nindent 4 }}
data:
  profiles.json: |
{{ dict "profiles" .Values.profiles | toPrettyJson | indent 4 }}
